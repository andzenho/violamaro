# kontent/kp/ — контент-план для Google-таблицы

Здесь лежит **источник правды** по контент-плану. Отсюда контент попадает в
Google-таблицу «Виола // Рабочее пространство», во вкладки КП — в две колонки:
`Тема / идея` и `Текст поста` (`Суть`). Остальное в таблице не трогаем.

Есть два способа перенести контент в таблицу. Начни с простого.

---

## Способ 1 (простой, без настроек) — скопировать и вставить

Ничего устанавливать и подключать не надо.

```
python3 skripty/kp_export.py
```

Скрипт соберёт весь контент и сохранит в `kontent/kp/_dlya_vstavki.txt`.
Открываешь этот файл — там по каждой дате два готовых блока:
① «Тема / идея» и ② «Текст поста». Копируешь каждый в нужную ячейку таблицы
(строку находишь по дате).

Плюс: ноль настройки. Минус: вставляешь руками.

---

## Способ 2 (автоматический) — позже, если захочешь

Скрипт `skripty/sheets_sync.py` сам пишет в таблицу (только в те же две
колонки, только в существующие строки, дизайн не ломает). Требует разовой
настройки доступа Google (сервис-аккаунт). Инструкция — в конце файла.
Пока не настроено — пользуйся Способом 1.

---

## Как это устроено (папки и файлы)

```
kontent/kp/
  <папка вкладки>/
    _tab.txt              # первая строка — ТОЧНОЕ имя вкладки в таблице
    2026-08-07--1.md      # посты; дата берётся из имени файла
    2026-08-07--2.md      # два поста на одну дату — суффикс --1, --2, ...
    2026-08-08.md         # один пост на дату
```

Папку можно называть как удобно — важно `_tab.txt` внутри с точным именем
вкладки.

### Формат файла поста

```
тема: идея поста и его цель, одной строкой
статус: план
---
Здесь текст поста — итоговый контент.

Многострочно, с пустыми строками между абзацами.
```

- `тема:` → колонка **Тема / идея**. Обязательна.
- Текст после `---` → колонка **Текст поста** (или **Суть**).
- Если текста ещё нет (только идея) — файл без `---` и без тела.
- Несколько постов на дату лягут в строки этой даты сверху вниз, в порядке
  имён файлов.

### Несколько постов на одну дату

Если на дату больше одного поста, суффикс `--N` обязателен у **всех** файлов этой даты: `2026-08-27--1.md`, `--2`, `--3`.

⚠️ Файл без суффикса (`2026-08-27.md`) при сортировке встаёт **после** `--2` и `--3`, и `sheets_sync.py` разложит посты по строкам таблицы в неверном порядке. Ошибка тихая: скрипт не ругается, просто путает утро с вечером.

## Статусы (канон, ассистент ведёт сам)

Жизненный цикл: **план → написан → опубликован**.

- `план` — есть тема/смысл, тела нет (или черновик).
- `написан` — итоговый текст готов и согласован продюсером в чате.
- `опубликован` — продюсер подтвердил публикацию (сказал прямо или кинул
  ссылку на пост). Ссылку на опубликованный пост дописывать в файл строкой
  `ссылка: <url>` под статусом.

Правила ведения (для ассистента, в любой сессии):
1. Статус обновляется **сразу** при смене состояния: написал текст по «ок»
   продюсера → `написан`; узнал о публикации → `опубликован` + `ссылка:`.
2. Не оставлять `план` у файла с готовым телом.
3. При каждой синхронизации перед `--apply` проверять, нет ли постов
   прошедших дат, застрявших в `написан`. **Спрашивать продюсера больше не нужно —
   сверяем скриптом** (см. ниже), он читает канал и сам находит расхождения.
4. Смена статуса — обычный коммит (без отдельного разрешения).

### Сверка с каналом: `skripty/tg_kanal.py`

Читает публичное веб-зеркало канала (`https://t.me/s/violamaro1`) обычным
HTTP-запросом. Ни бота, ни токенов, ни постоянно работающего процесса.

```
python3 skripty/tg_kanal.py                  # сухой прогон: что вышло и что не сходится
python3 skripty/tg_kanal.py --apply          # проставить «опубликован» и «ссылка:»
python3 skripty/tg_kanal.py --list           # просто лента канала с просмотрами
python3 skripty/tg_kanal.py --limit 200      # заглянуть глубже (по 20 постов за запрос)
python3 skripty/tg_kanal.py --kp "КП Ютуб"   # сверить другую папку
```

Сопоставляет посты по тексту, не по дате, — поэтому находит и те, что вышли
не в свой день. Показывает четыре группы: статус в КП другой (настоящие
расхождения), не хватает только ссылки, числятся готовыми но в канале нет,
есть в канале но нет в КП. `--apply` трогает только строки `статус:` и
`ссылка:` в шапке, тело поста не изменяется.

⚠️ **Только публичные каналы.** Закрытые («Кабинет предзаписи», старый канал,
бот) веб-зеркала не имеют — там по-прежнему HTML-экспорт и `tg_comments.py`.

⚠️ Расхождения из групп «числятся готовыми, но не найдены» и «есть в канале,
нет в КП» скрипт **не правит сам** — их разбирает ассистент вручную: пост мог
быть отредактирован при публикации, выйти без КП или быть удалён.

---

## Настройка Способа 2 (сервис-аккаунт Google) — полностью авто

Разовая настройка (~10 минут). Карта/оплата **не требуются** — окно про
«бесплатный период $300» просто пропускается. После настройки посты пишутся
в таблицу автоматически, без ручной вставки.

1. [Google Cloud Console](https://console.cloud.google.com/) → создать проект.
2. Включить **Google Sheets API**.
3. Создать **сервис-аккаунт**, у него ключ **JSON**, скачать.
4. В таблице → «Настройки доступа» → добавить **email сервис-аккаунта**
   (`...@....iam.gserviceaccount.com`) с ролью **Редактор**.
5. Отдать ключ скрипту — любым из двух способов:
   - **переменная окружения** `VIOLA_SHEETS_CREDS_JSON` = содержимое JSON-файла
     (рекомендуется: ключ не лежит в репозитории; так его удобно задать в
     настройках окружения Claude Code, чтобы синхронизацию делал ассистент);
   - **или файл** `skripty/.google-creds.json` (он в `.gitignore`).
6. Библиотеки: `python3 -m pip install -r skripty/requirements-sheets.txt`

Запуск (или это делает ассистент):

```
python3 skripty/sheets_sync.py --list-tabs   # проверить вкладки
python3 skripty/sheets_sync.py               # сухой прогон: что изменит
python3 skripty/sheets_sync.py --apply       # записать в таблицу
```
