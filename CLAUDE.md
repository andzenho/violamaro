# Проект «Виола Маро»

<!-- Правила по разделам вынесены в .claude/rules/*.md — они подгружаются
     сами, когда ассистент открывает файл в соответствующей папке.
     История решений — baza/zhurnal-resheniy.md. Карта файлов — karta.md.
     Здесь держим только то, что нужно в КАЖДОЙ сессии. Порог — 40 строк. -->

## О проекте
Запуск линейки онлайн-продуктов психотерапевта Виолы Маро. Виола — эксперт и методолог, живёт в Италии; психолог, психотерапевт («эмпатолог» — слово аудитории, в наши тексты не идёт). Продюсер (я) — маркетинг, продажи, тексты. Артём — контент и трафик, Аня — документы и продажи.

Запуск август–октябрь 2026: лид-магнит → событие 1990 ₽ в сентябре → флагманский практикум осенью. Воронка: короткие видео и YouTube → Telegram-канал `violamaro1` → анкета предзаписи → продажи.

**Действующий курс.** Флагман — практикум **«Прикладная эмпатия»** под платёжеспособного эмпата: «эмпатия-суперсила → отношения и деньги», тарифы 14 900 / 29 900. Вина и жертва — только дешёвый вход. Прогрев под рамкой «Я ЭМПАТ». Почему так — `baza/zhurnal-resheniy.md`.

## Куда идти
`baza/` голос и каноны (правда по смыслу) · `korpus/` сырьё, тяжёлое · `produkt/` программы и цены · `analitika/` ЦА и цифры · `kontent/kp/` контент-план · `docs/` опубликованные страницы · `skripty/` инструменты · `arhiv/` **не читать без просьбы**.

Полная карта с весом каждого файла — **`karta.md`**, пересобрать: `python3 skripty/karta.py`. Детальные правила раздела подгрузятся сами при открытии файла (`.claude/rules/`).

## Философия: не усложнять (решение продюсера 03.09)

Одна тема — один хозяин-файл. Один фреймворк, не одиннадцать. Сложность, которая
за месяц не изменила ни одного решения, удаляется.

**Остаётся только то, что окупается:** голос Виолы (читается целиком), красные
линии (её требования), кодекс честности (юридический риск и её репутация).
Всё остальное обсуждаемо и режется.

**Ассистент помечает каждое утверждение:** проверено данными или это его чтение.
Без пометки продюсер будет одобрять догадки как факты — так уже случалось.

Лимитов на объём ответов и шапок нет: пишем сколько нужно, режет продюсер.

## Правило воронки текста (обязательно)
Перед написанием любого текста от лица Виолы — прочитать `baza/golos-viola.md` **целиком** и **один** нужный тематический файл базы. **Не писать по памяти.**

## Кодекс честности (обязательно, все каналы)
**Отказ от считываемых продающих манипуляций.** Тест «в лицо»: можем объяснить приём аудитории и не покраснеть — оставляем, нет — выбрасываем. Запрещены фальшивые фидлеры, непроверяемые цифры спроса, пустые петли, каскады дожимов, продление дедлайнов. Дефицит и дедлайны — только реальные и с названной причиной. Слово «техники», не «приёмы»; прошлые программы Виолы не обесценивать. Полный кодекс — `kontent/kp/strategiya-kontent-plana.md §0, §8а`.

## Красные линии (кратко; подробно — `.claude/rules/baza.md`)
- Обещания результата **разрешены** (запрет снят 21.08), но помечать для продюсера.
- Регалии: в 3-м лице можно, в прямой речи Виолы от первого лица — нет.
- Без заочных диагнозов. Клиентские случаи обезличивать. Двойная гендерная разметка ролей. Термин — только после бытового объяснения.
- В базе одна метка: `[Д]` — её дословные слова, их можно в текст как есть. Всё без `[Д]` — не её слова, от её лица не публикуем. Старые `[П]` и `[В]` читаются как «не её».

## Парадигма продуктов (сквозное, во всех текстах)
Продукт и текст — это **трансформация состояния человека**: энергии, тела, позиции, голоса, её образами (плита → свет). Не «тема/работа с виной», а метаморфоза «из А в Б», внутренняя (не «обстоятельства изменятся»). Заголовок и оффер = обещание сдвига состояния. Продаём метаморфозу `эмпат-жертва → исцелившийся эмпат`, линза «энергия/сила», враг-витрина — токсик/манипулятор.

**Ведущий фрейм (её словами, 02.09):** сила **утекает через решето**, потому что [Д] «система выживания эмпата — это быть удобным для всех». Задача — [Д] «найти и не потерять, никому не отдавать». Дар остаётся, меняется адресат: [Д] «зажечь свет во всех своих комнатах». Прежняя формулировка «суперсила, заблокированная виной» снята: «суперспособность» придумал продюсер, «заблокированная виной» в её речи не встречается. **Вина — механизм внутри, не мотив покупки** ([Д] «это не основной триггер точно»); двигатель — неспокойствие. «Суперсила» разрешена в письме от её лица, ведущим фреймом не является. Слово «эмпат» разрешено и в теле текста. Канон и сетка — `produkt/transformatsii.md`, `produkt/transformatsiya-za-dengi.md`.

## Конструкция «не X, а Y»
**Запрещена** в продающих текстах. **Разрешена** в экспертном контенте от лица Виолы — это её собственный способ мышления.

## Как отвечать продюсеру
Короткие структурированные блоки с заголовками. Без лишних пояснений и без похвалы. Давить критикой, а не соглашаться; не менять позицию без новых данных. Проверяемое — считать скриптом, а не на глаз.

## Экономия на каждом запросе (обязательно)
- **Не перечитывать прочитанное в этой сессии.** Файл в контексте — работать по нему.
- **Файл целиком — только когда нужен весь файл.** Иначе `grep -n`, потом `Read` с `offset`/`limit`. Вес — `python3 skripty/ves.py <путь>`; тяжелее ~10 тыс. токенов — кусками. Исключение — `baza/golos-viola.md` (правило воронки).
- **Один тематический канон, а не все.**
- **Подагентов не звать** на задачу в два-три файла: подагент стартует с пустого контекста и перечитывает всё заново.
- **Независимые команды — одним сообщением.**
- **Не печатать то, что и так видно** в файле, диффе или выводе скрипта.

## Правило «В базу?»
Появился материал, который стоит добавить в базу, — спросить отдельным блоком в конце ответа. Без подтверждения в базу ничего не писать.

## Git и синхронизация
1. Источник правды — ветка `main`: https://github.com/andzenho/violamaro
2. `git pull` в начале сессии — SessionStart-хук. Хук молчит, когда всё свежее; пришли изменения — коротко сообщить.
3. **Авто-сохранение рабочей ветки.** Stop-хук (`skripty/avtosohran.sh`) сам коммитит и пушит рабочую ветку после каждого ответа — машина сессии временная. Спрашивать разрешения на это не надо, отдельно сообщать об этом тоже. **В `main` хук не ходит никогда**: слияние ветки в main — только по прямому «да» продюсера. Осмысленный коммит (п. 4, конец смыслового блока) пишем как раньше, руками и с нормальным сообщением; авто-коммиты — страховка между ними, а не замена решения.
4. Обновил файл в `baza/` — отдельный коммит с указанием источника.
5. Коммит и пуш идут вместе. Не оставлять незапушенных коммитов после «да».
6. Расхождение веток сам не разрешать: показать обе версии и спросить.
7. Ветку под задачу заводим одну, от свежего `main`. После слияния удаляем локальную. **Ветка на GitHub остаётся — окружение сессии не даёт удалять удалённые ветки.** Это не проблема и не повод для отчёта: слитая ветка ничего не содержит сверх `main`. Не пытаться удалить, не предлагать удалить, не упоминать. Захочет продюсер прибраться — сделает на GitHub сам.
8. Машина сессии временная, но за сохранность отвечает хук из п. 3 — напоминать продюсеру про коммит не надо. Хук подал голос (пуш не прошёл, мы на `main`) — сказать одной строкой.
