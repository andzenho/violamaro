# Что graphify НЕ индексирует в этом репозитории.
# Правило то же, что в CLAUDE.md: в граф попадает только то, что можно
# показывать как действующий источник правды.

# Архив — устаревшее и противоречащее текущему курсу (старый флагман «Я выбираю
# себя», ЦА по вине, снятые офферы). Если пустить его в граф, мёртвые связи
# всплывут в ответах наравне с живыми и разворот 11.08 будет размыт.
arhiv/

# Служебный код. AST-разбор питоновских скриптов даёт узлы-функции,
# которые засоряют смысловой граф и ни на один рабочий вопрос не отвечают.
skripty/

# Опубликованная страница теста — дубль смысловых исходников produkt/test-empat*.md.
docs/

# Конфиг ассистента, не контент.
.claude/

# Выход самого graphify.
graphify-out/
