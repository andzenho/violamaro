# Системный мусор
.DS_Store
Thumbs.db
desktop.ini

# Временные файлы
*.tmp
*.temp
*.swp
*.bak
*~

# Ключ сервис-аккаунта Google для sheets_sync.py — НИКОГДА не коммитить
skripty/.google-creds.json
.google-creds.json

# Node (зависимости для skripty/sait.js ставятся локально, не коммитим)
node_modules/
skripty/package-lock.json

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/
.mypy_cache/
.pytest_cache/

# Генерируемый файл для ручной вставки (Способ 1)
kontent/kp/_dlya_vstavki.txt

# Сырые дампы комментариев (регенерируются skripty/yt_comments.py, не коммитим)
# в т.ч. в подпапках (vina/ и т.п.) — в репозитории держим только разбор
analitika/kommentarii/*.jsonl
analitika/kommentarii/*.txt
analitika/kommentarii/**/*.jsonl
analitika/kommentarii/**/*.txt
analitika/kommentarii/*.json
analitika/kommentarii/**/*.json
# Файл-контекст для ассистента вне Claude Code (генерируется kontekst_pak.py)
viola-kontekst-dlya-assistenta.md

# Ключ AssemblyAI для skripty/transkript.py — НИКОГДА не коммитить
.assemblyai-key

# Сырые ответы AssemblyAI (пословные таймкоды, тяжёлые). Индекс — коммитим.
.transkript/otvety/
