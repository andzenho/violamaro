# Системный мусор
.DS_Store
Thumbs.db
desktop.ini

# Временные файлы
*.tmp
*.temp
*.swp
*.bak
*~

# Ключ сервис-аккаунта Google для sheets_sync.py — НИКОГДА не коммитить
skripty/.google-creds.json
.google-creds.json

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/
.mypy_cache/
.pytest_cache/

# Генерируемый файл для ручной вставки (Способ 1)
kontent/kp/_dlya_vstavki.txt

# Сырые дампы комментариев (регенерируются skripty/yt_comments.py, не коммитим)
# в т.ч. в подпапках (vina/ и т.п.) — в репозитории держим только разбор
analitika/kommentarii/*.jsonl
analitika/kommentarii/*.txt
analitika/kommentarii/**/*.jsonl
analitika/kommentarii/**/*.txt
analitika/kommentarii/*.json
analitika/kommentarii/**/*.json

# npm (docx-генератор ставится локально)
node_modules/
package-lock.json
