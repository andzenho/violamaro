Сделай то, что горит по контент-плану. Без вопросов и без разбора — на выходе готовые тексты.

Аргумент необязателен: без него берём сегодняшний день, с ним — то, что назвали (`завтра`, `29.08`, `просрочку`, `КП Бот рассылки` — имя вкладки, не папки).

Порядок:
1. `python3 skripty/plan.py` — увидеть, что горит. Дальше без разведки: пути к файлам уже есть в выводе.
2. Отбери, что делать: сегодняшнее и просроченное в статусе `план`/`идея`. Посты в статусе `написан` **не переписывай** — они готовы, у них просто не проставлен `опубликован`; собери их отдельным списком и спроси продюсера одной строкой.
3. Прочитай `baza/golos-viola.md` целиком **один раз на всю пачку** и **один** тематический канон по теме (список — в `/post`). Не перечитывать на каждый пост.
4. По каждому посту:
   - напиши текст по правилам `/post` (дословные `[Д]`, метку `[В]` не использовать, красные линии, кодекс честности, лимиты Telegram);
   - впиши текст в тот же файл поста, статус смени `план` → `написан`;
   - прогони `python3 skripty/proverka.py` по файлу.
5. Закоммить и запушить всё разом, одним коммитом на пачку.
6. Покажи в ответе **только готовые тексты** и одну строку итога: сколько написано, что осталось, какие вопросы к продюсеру. Метрики `proverka.py` — только по тем постам, где есть нарушения. Никаких отчётов о процессе.

Правила, которые не обходим:
- В базу ничего не пишем — это отдельное решение продюсера.
- Обещания результата разрешены, но каждое помечаем для его внимания.
- Дата в прошлом и статус `написан` — сигнал, что пост не опубликовали. Это вопрос к продюсеру, а не повод переписать.
