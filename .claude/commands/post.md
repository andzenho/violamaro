Напиши пост для Telegram-канала Виолы (violamaro1).

Порядок:
1. Перед написанием прочитай целиком `baza/golos-viola.md` и `baza/kanon-viny.md`. Не пиши по памяти.
2. Строй текст на её дословных формулировках с меткой `[Д]` из базы. Свои связки между её фразами — минимальны.
3. Соблюдай красные линии из CLAUDE.md: без обещаний результата, без стажа и регалий, без заочных диагнозов, клиентские случаи обезличены, термин — только после бытового объяснения.
4. Метку `[В]` (невподтверждённый вывод) не использовать вообще.
5. После текста прогони его через `python3 skripty/proverka.py <файл>` и покажи метрики вместе с текстом.
6. Ниже разбора отдельным блоком укажи: какие фрагменты — её дословные `[Д]`, а какие — наши связки.

Тему поста возьми из аргумента команды. Если темы нет, спроси.
Держись в лимите Telegram: подпись к видео 1024 знака, обычный пост 4096.
