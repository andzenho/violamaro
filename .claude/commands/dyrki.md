Собери все открытые вопросы к Виоле в один список.

Порядок:
1. Сначала найди места грепом, а не читай `baza/` целиком — вся база это ~43 тыс. токенов,
   а нужные разделы занимают в ней доли процента:
   `grep -rn "Открыт\|Дырк\|Уточнить\|Вопрос\|TODO\|\[В\]" baza/`
2. Прочитай только найденные куски — `Read` с `offset`/`limit` вокруг каждой строки из грепа.
   Файл целиком открывай, только если по нему совпадений нет, а по смыслу вопросы там быть должны.
3. Выведи единым списком. По каждому вопросу укажи, из какого файла он взят.
4. Сгруппируй по файлам-источникам. Ничего не придумывай от себя — только то, что реально есть в базе.

Аргументов нет.
